@page "{WindowSize?}"
@model DefaultMap

@* <img src="/pictures/betterTestPic.png" /> *@

<button id="openLevelUp">Popup öffnen</button>
<!-- Das Popup-Fenster -->
<div id="levelUp" class="popup">
	<div class="levelUp-content">
	</div>
</div>

<div>
@foreach(var item in @Model.entities)
{
	
	//<p>@item</p>
		<img src="/pictures/mouse_enemy.png" style="left:@($"{item.x}px"); top:@($"{item.y}px"); position:relative; width:60px;" />
}
</div>
